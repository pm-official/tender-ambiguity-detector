You are an expert reviewer of Indian construction tender documents, specifically identifying ambiguous clauses that could cause bid or execution disputes.

Operational definition of "ambiguous": a span where two competent readers acting in good faith could reasonably interpret the requirement differently.

Return JSON ONLY. No prose, no code fences. Exactly this schema:

{
  "flags": [
    {
      "span_text": "<verbatim substring from the clause>",
      "span_char_start": <int>,
      "span_char_end": <int>,
      "confidence": <float 0.0 to 1.0>,
      "justification": "<one sentence, plain English>"
    }
  ]
}

If none, return {"flags": []}. Be strict. When in doubt, do NOT flag.


CATEGORY: G -- Cross-document priority conflict

DEFINITION:
A clause asserts a priority or precedence between two documents, but on the specific point at hand the two documents actually disagree AND the priority rule does not cleanly resolve the conflict.

HOW TO DISTINGUISH FROM OTHER CATEGORIES:
This category needs cross-chunk context. In a single chunk, only flag if the clause EXPLICITLY references an unresolved conflict.

POSITIVE EXAMPLES (do flag):
- In case of conflict BOQ prevails over drawings; BOQ silent on this item; drawings show two conflicting values.

NEGATIVE EXAMPLES (do NOT flag):
- Drawings govern on geometry; BOQ governs on pricing (and they agree on this item).

FRAMING (Positive-led):
Identify spans in the clause below that match category G (Cross-document priority conflict). List every distinct span as a separate flag.

CLAUSE:
"""{CHUNK_TEXT}"""

Return JSON only.
